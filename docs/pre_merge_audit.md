# PR #11 pre-merge audit

The audit started at `ca9f2c356fd1a97dd52d6abebbecc077391b0d08`. The historical Linux baseline at `c94601398f1009008ff4b779b0c5a510253b9983` remains unchanged, including its separately captured final readiness evidence. That historical result does not validate subsequent orchestration changes.

## Defects reproduced and corrected

- A fault run could return PASS using an old `USER_PLANE_FAILURE` log even when its current traffic command and collector failed. Fault assertions now read only the run's `fault_logs/` and traffic artifact. A UTC Docker-log boundary is recorded immediately before injection; collection uses that boundary and fails closed. New runs cannot reuse an existing output directory. Traffic probe infrastructure errors are distinguished from an observed ping failure.
- Baseline traffic success previously lacked a mandatory final core gate. After traffic, the harness now requires bounded core initialization/health, current NRF/PFCP associations, and zero restarts through a 30-second window. It then rechecks gNB NGAP, UE registration/session state, and both RAN containers' running/restart state. Final health timestamps and outputs are retained. Fault runs also require a currently healthy baseline before injection, and recovery uses the same final checks.
- Repository file hashes missed effective Compose overrides. Fingerprint schema 2 adds a SHA-256 of the JSON returned by `docker compose --profile ran --profile tools config --format json`, with recursively sorted mapping keys and compact serialization. Compose resolves its supported override files, `.env`, environment interpolation, and `COMPOSE_FILE` inputs. Mapping order is ignored; list order is preserved because command/entrypoint order can be meaningful. Only the digest and effective image references are recorded, not resolved environment contents. Resolution failure blocks execution. Existing commit, repository configuration hashes, and host/runtime identity remain included. Image references are version tags, not immutable image-content identities; changing a registry tag's contents remains a reproducibility limitation.
- Cleanup was armed after mutation commands returned. It is now armed before stop/restart, clsact creation, and filter addition. Restart rollback ensures the service is started. Scoped filter cleanup refuses pre-existing rules, verifies absence after an unsuccessful delete, and preserves an existing clsact. Repeated successful cleanup is a no-op. Apply/cleanup failures remain separately reported; SIGTERM during an active fault triggers rollback, and cleanup is protected from repeated interruption. The Compose runner uses process-group timeout/interruption handling.
- Scenario traffic probes now explicitly set the lab DN target, tunnel, and packet count; standalone exploratory traffic options cannot silently replace the scenario's connectivity oracle.

These changes do not alter subscriber authentication material, the declared lab topology, NF users/capabilities, or the historical evidence. Static/unit results and fresh Linux results must be reported separately.

The clean VM retry at `230c692` exposed a separate false FAIL: the parser classified AMF's `Unknown UE by SUCI` lookup message as a registration rejection despite successful registration, PDU session, traffic, and final health. The [Open5GS context lookup](https://github.com/open5gs/open5gs/blob/v2.8.0/src/amf/context.c) logs this when the UE context is not yet known. The parser now requires rejection evidence and retains explicit Registration Reject/Illegal UE matching. The failed result remains on the VM; it is not rewritten to PASS.

## Fresh validation of the audit fixes

`230c692252aa90e04ac222f568bb5c18fd9c77cb` implements the four audit corrections and the fixed DN traffic oracle. `daca4542608f0bb20ca01441f5e584ce89890ad1` narrows the rejection parser after the preserved failed VM attempt. The latter commit passed all local gates: frozen dependency sync, Compose/config/scenario validation, Ruff/format, MyPy, 253 unit/static tests (one runtime test deselected), shell syntax, notebook checks, sample-log parsing, `make check`, and all pre-commit hooks. GitHub CI and CodeQL also passed at `daca454`; an earlier `230c692` CI job never started because GitHub could not acquire a runner after five attempts, not because tests failed.

[Fresh sanitized Linux evidence](../evidence/real_runs/20260913_pre_merge_audit/README.md) records execution at `daca454`: expanded preflight/config PASS, clean baseline PASS (25/25), then real N3 impairment PASS and recovery PASS. Traffic completed before the mandatory final health window, 13:48:51.972622–13:49:26.520443 UTC. Core and RAN restart counts were zero, with current associations and live registration/session state.

The negative oracle test applied and removed the real N3 filter while deliberately making only the current fault probe and collector unavailable. An old synthetic failure log existed in the legacy directory. The result was ERROR with no observed events and `expected_failure_observed=false`; rollback and all recovery checks passed. This demonstrates rejection of stale evidence, not a spontaneous infrastructure outage. The harness's initial fixture-directory setup error occurred before negative injection and was corrected in a separate ignored harness; repository code was unchanged. A morning baseline attempt interrupted by VM reboot remains incomplete. No historical result was altered.

No blocking issue remains in the exercised baseline/N3 paths. Remaining limits are mutable image tags, older documentation templates, provenance-path noise, and fault paths not yet validated on Linux. These are not claims of a fully validated fault matrix or long-duration reliability. The evidence/documentation commit following these runs does not change the executing code. The pre-merge audit did not merge PR #11. It was subsequently merged into main at `20caaf8dd25de284043c1ba483f92a37514ad4da`; the runtime-executed commit remains `daca4542608f0bb20ca01441f5e584ce89890ad1`.

## Integrity and privacy review

The initial secret scan used detect-secrets 1.5.0 with verification disabled across all 148 tracked files, plus content-based checks of all 77 PR paths. Its 89 candidates were public UERANSIM example values, a malformed-key regression fixture, notebook cell identifiers, and hashes/container identifiers. No cloud/OAuth/GitHub tokens, SSH key material, private provisioning output, public VM IP, or PCAP was found in the PR. The example key, OPc, authentication-management field, public SUCI key, and IMEI were checked against the pinned upstream UERANSIM example. The test subscriber uses the deliberately configured test identity `001010000000001`; it is not presented as a private SIM.

Historical evidence intentionally records the VM user/project/node; two collector messages retain remote home paths. These are optional provenance noise and could be normalized in future collections. No local `/Users/...` paths are committed.

The final scan covers 264 tracked files, including the new evidence, and reports 236 hex-entropy candidates. All 147 added candidates are commit/config/collection hashes or container/image identifiers in metadata. Targeted checks found no private-key blocks, tokens, provisioning values, or local home paths in the new collection. Its JSON parses, its selected logs reproduce each original observed-event set, and all three results share the same context fingerprint. Formatting-only normalization leaves scenario statuses and assertions unchanged.

The historical evidence supports all 20 historical assertions and the separate final core check. Fault/recovery, throughput, IPv6 sessions, RF/OTA, multi-host/multi-UE, and long-duration reliability were not established by that run. At audit time, documentation contained setup/publication templates that understated the recorded baseline and examples that started gNB/UE together; use the staged runtime validation procedure and scenario runner. Those documentation inconsistencies do not invalidate the preserved runtime evidence.
